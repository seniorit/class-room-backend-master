const defaultMessages = {
  createdMessage: 'Record Created successfully',
  updatedMessage: 'Record Updated Successfully',
  deletedMessage: 'Record Deleted Successfully'
}

const { createdMessage, updatedMessage, deletedMessage } = defaultMessages

module.exports = {
  createdMessage,
  updatedMessage,
  deletedMessage
}
